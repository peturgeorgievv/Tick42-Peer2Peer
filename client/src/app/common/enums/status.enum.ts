export enum StatusENUM {
  current = 'current',
  paid = 'paid',
  requestOpen = 'request-open',
  requestClosed = 'request-closed',
  suggestionPending = 'suggestion-pending',
  suggestionAccepted = 'suggestion-accepted',
  suggestionRejected = 'suggestion-rejected'
}
